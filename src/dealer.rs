use base::{Card, NumberCard};
#[derive(Debug)]
pub struct Dealer {
    /// 108张牌
    cards: [Card;108],
}
impl Dealer {
     /// 新建一个发牌家
     /// 数字牌（76张）、功能牌（24张）、万能牌（8张）
    //  pub fn new() -> Dealer {
    //     (NumberCard::One)
    //  }
 }
